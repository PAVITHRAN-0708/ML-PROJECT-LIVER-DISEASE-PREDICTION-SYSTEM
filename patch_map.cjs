const fs = require('fs');
let code = fs.readFileSync('src/App.tsx', 'utf8');

if (!code.includes("import HospitalMap")) {
    code = code.replace("import { jsPDF } from 'jspdf';", "import { jsPDF } from 'jspdf';\nimport HospitalMap from './HospitalMap';");
}

const targetHtml = `<div className="disclaimer" style={{ background: '#fff0f0', border: '1px solid #ffcfcf', padding: '16px', borderRadius: '8px', marginTop: '24px', color: '#c53030' }}>
              <strong style={{ display: 'block', marginBottom: '4px' }}>⚠️ Not Medical Advice</strong>
              Educational AI tool. This analysis is generated by a machine learning model based on clinical guidelines. It may contain errors and is NOT a definitive diagnosis. Do not rely on this app for personal medical decisions. Clinical correlation by a licensed physician is required.
            </div>`;
            
const replacementHtml = `<div className="disclaimer" style={{ background: '#fff0f0', border: '1px solid #ffcfcf', padding: '16px', borderRadius: '8px', marginTop: '24px', color: '#c53030' }}>
              <strong style={{ display: 'block', marginBottom: '4px' }}>⚠️ Not Medical Advice</strong>
              Educational AI tool. This analysis is generated by a machine learning model based on clinical guidelines. It may contain errors and is NOT a definitive diagnosis. Do not rely on this app for personal medical decisions. Clinical correlation by a licensed physician is required.
            </div>
            
            <div style={{ marginTop: '32px' }}>
              <h3 style={{ fontSize: '18px', color: '#1a1d20', marginBottom: '8px' }}>Nearby Medical Test Centers</h3>
              <p style={{ color: '#4a5568', fontSize: '14px', marginBottom: '16px' }}>Find the nearest clinics and hospitals to get a liver function panel test.</p>
              <HospitalMap />
            </div>`;

code = code.replace(targetHtml, replacementHtml);
fs.writeFileSync('src/App.tsx', code);
